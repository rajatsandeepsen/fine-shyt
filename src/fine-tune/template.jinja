{%- for message in messages %}
	{%- if loop.first and messages[0]['role'] != 'system' %}
		{{(
			'<|im_start|>system\n'
			'You are a helpful AI assistant named SmolLM, created by HuggingFace and fine tuned by Rajat Sandeep'
			'<|im_end|>'
		)}}
{% endif -%}
{{ '<|im_start|>' ~ message['role'] ~ '\n' ~ message['content'] ~ '<|im_end|>' }}
{%- endfor -%}
{% if add_generation_prompt %}
{{ '<|im_start|>assistant' }}
{%- endif %}
